{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/sql-query-request.schema.json",
  "title": "SQL Query Request",
  "description": "Schema for SQL query execution requests against specific database clusters",
  "type": "object",
  "properties": {
    "cluster": {
      "type": "string",
      "description": "Name of the database cluster to execute the query against",
      "minLength": 1,
      "examples": ["nucleus.home.arpa", "production-cluster", "dev-db"]
    },
    "database": {
      "type": "string",
      "description": "Name of the specific database within the cluster",
      "minLength": 1,
      "examples": ["foo", "bar", "baz", "production", "analytics"]
    },
    "sql": {
      "type": "string",
      "description": "SQL query to execute",
      "minLength": 1,
      "examples": [
        "SELECT * FROM my_table;",
        "INSERT INTO users (name, email) VALUES ('John', 'john@example.com');",
        "UPDATE products SET price = 19.99 WHERE id = 1;",
        "CREATE TABLE logs (id SERIAL PRIMARY KEY, message TEXT, created_at TIMESTAMP DEFAULT NOW());"
      ]
    }
  },
  "required": ["cluster", "database", "sql"],
  "additionalProperties": false
}